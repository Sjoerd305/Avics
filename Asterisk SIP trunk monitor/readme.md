Asterisk script to check if SIP trunk is currently registerd. If SIP trunk is not registered, the script will perform an asterisk core restart. 

Crontab version is stable, running it as a service can be a problem on older CentOS installations. 